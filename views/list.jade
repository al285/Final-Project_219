doctype html
html
  head
    title= title
    link(rel="stylesheet", href="stylesheets/header.css")
  body
    include header.jade
    ul
      each value, key in rec
        li
          a(href="/#{route}/#{value._id}") #{value.name}